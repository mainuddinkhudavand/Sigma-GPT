import { createContext } from "react";

export const THEMES = {
  DARK: "dark",
  CYBERPUNK: "cyberpunk",
  GLASSMORPHISM: "glassmorphism",
  EMERALD: "emerald"
};

export const MyContext = createContext(null);